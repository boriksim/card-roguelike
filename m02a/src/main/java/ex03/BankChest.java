package ex03;

/*
 * Урок 2a.3 — «Инкапсуляция».
 *
 * Банковский сундук гильдии. Золото внутри — private: снаружи класса
 * его нельзя тронуть напрямую, только через методы. Твоя задача —
 * написать методы так, чтобы ГЛАВНОЕ ПРАВИЛО сундука (инвариант)
 * не нарушалось никогда: золота не бывает меньше нуля.
 *
 * Проверка — JUnit-тесты: m02a/src/test/java/ex03/BankChestTest.java
 * (не забудь убрать там @Disabled).
 */
public class BankChest {

    // ШАГ 1. Объяви private-поле gold типа int — запас золота.
    // Снаружи класса оно не видно: строчка chest.gold = -100; в чужом
    // коде даже не скомпилируется. В этом и смысл инкапсуляции.
    // TODO: объяви поле

    // ШАГ 2. Геттер — единственный способ ПОСМОТРЕТЬ золото снаружи.
    public int getGold() {
        // TODO: замени заглушку — верни поле
        return -1;
    }

    // ШАГ 3. Положить золото. Класть можно только положительное
    // количество: amount <= 0 молча игнорируем.
    public void deposit(int amount) {
        // TODO
    }

    // ШАГ 4. Забрать золото. Правила:
    //   - если просят больше, чем есть — отдай всё, что есть;
    //   - если amount <= 0 — не отдавай ничего;
    //   - верни, сколько РЕАЛЬНО удалось забрать.
    // После любого вызова золота в сундуке >= 0 — за этим следит
    // только этот метод, и потому правило невозможно обойти.
    public int withdraw(int amount) {
        // TODO: замени заглушку
        return -1;
    }
}
