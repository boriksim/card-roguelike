package ex02;

/*
 * Урок 2a.2 — «Конструкторы и this».
 *
 * Кузница выдаёт оружие. Поля уже объявлены, а вот конструкторы пусты —
 * сейчас любое оружие выходит из кузницы безымянным и с нулями.
 *
 * Проверка — JUnit-тесты: m02a/src/test/java/ex02/WeaponTest.java
 * (не забудь убрать там @Disabled).
 */
public class Weapon {

    String name;
    int damage;
    int durability; // прочность: сколько ударов оружие выдержит

    // ШАГ 1. Допиши конструктор: сохрани параметры в поля объекта.
    // Имена параметров совпадают с именами полей, поэтому поле называй
    // через this: this.name = name; — слева поле, справа параметр.
    public Weapon(String name, int damage, int durability) {
        // TODO: три присваивания
    }

    // ШАГ 2. Второй конструктор — для оружия со стандартной прочностью 10.
    // Не копируй присваивания из первого конструктора: вызови его
    // строкой this(name, damage, 10); — она должна быть ПЕРВОЙ (и здесь
    // единственной) строкой конструктора.
    public Weapon(String name, int damage) {
        // TODO: одна строка
    }

    // ШАГ 3. Удар: верни damage и уменьши durability на 1.
    // Сломанное оружие (durability == 0) бьёт на 0, и прочность
    // дальше не падает — в минус она не уходит.
    public int strike() {
        // TODO: замени заглушку
        return -1;
    }
}
