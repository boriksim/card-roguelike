package ex08;

/*
 * Урок 1.8 — мини-проект «Бой 1-на-1» (v0.1).
 *
 * ГЛАВНАЯ работа этого урока — в модуле game (game/src/main/java/game/Main.java):
 * пройди чек-лист урока (энергия, выбор карт по номеру, намерение врага,
 * цвета, победа/поражение), добавь в руку СВОЮ четвёртую карту
 * и закоммить результат с пометкой v0.1.
 *
 * А здесь — три маленькие чистые функции боевой логики для разминки.
 * Такие же живут в Main.java: сначала напиши свои, потом сверь подход.
 *
 * Проверка — JUnit-тесты: m01/src/test/java/ex08/ExerciseTest.java
 * (убери там @Disabled).
 */
public class Exercise {

    // ШАГ 1. Хватает ли энергии на карту.
    public static boolean canPlay(int cardCost, int energy) {
        // TODO: замени заглушку
        return false;
    }

    // ШАГ 2. Намерение врага на ход: каждый третий ход (3, 6, 9, ...)
    // враг уходит в защиту, в остальные — атакует.
    // Верни строку "DEFEND" или "ATTACK".
    public static String enemyIntent(int turnNumber) {
        // TODO: замени заглушку
        return "";
    }

    // ШАГ 3 (со звёздочкой). Итог боя:
    //   "HERO"   — победил герой (враг пал),
    //   "ENEMY"  — победил враг (герой пал),
    //   "NOBODY" — оба живы, бой продолжается.
    // Если пали оба, победа засчитывается врагу: "ENEMY".
    public static String winner(int heroHp, int enemyHp) {
        // TODO: замени заглушку
        return "";
    }
}
