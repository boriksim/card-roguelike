package ex01;

/*
 * Урок 2b.1 — «Интерфейсы».
 *
 * Notifier — контракт: «умею доставлять сообщения». КАК доставлять —
 * интерфейсу всё равно: у него нет ни полей с данными, ни тел методов.
 * Каждый класс-реализация отвечает по-своему.
 *
 * Этот файл менять не нужно. Твоя работа — в EmailNotifier и SmsNotifier
 * рядом. Проверка: m02b/src/test/java/ex01/NotifierTest.java
 * (открой тест и убери строку @Disabled).
 */
public interface Notifier {

    // Доставить текст и вернуть строку-отчёт о доставке.
    String deliver(String text);

    // Короткое имя канала, например "email" или "sms".
    String channel();
}
