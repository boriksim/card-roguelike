package ex06;

/*
 * Урок 2b.6 — «Исключения: throw».
 *
 * Делёж добычи после боя. Метод сам ЗАЩИЩАЕТСЯ от бессмысленных
 * аргументов: не выдумывает ответ, а бросает исключение — пусть
 * вызывающий код узнает об ошибке сразу и громко.
 *
 * Проверка: m02b/src/test/java/ex06/LootSplitterTest.java (убери @Disabled).
 *
 * ШАГ 1. split: верни, сколько монет достанется КАЖДОМУ из members
 * участников (целочисленное деление, остаток пропадает).
 * Но сначала проверь аргументы:
 *   - если coins < 0    — throw new IllegalArgumentException("Монет не может быть меньше нуля");
 *   - если members <= 0 — throw new IllegalArgumentException("Делить добычу не на кого");
 * Текст сообщений можешь менять — главное, чтобы он был не пустой
 * и объяснял, что пошло не так.
 */
public class LootSplitter {

    public static int split(int coins, int members) {
        // TODO: замени заглушку
        return 0;
    }
}
