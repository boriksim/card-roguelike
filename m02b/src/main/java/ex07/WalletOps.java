package ex07;

/*
 * В переменной типа Wallet лежит НЕ сам кошелёк, а ссылка на него —
 * «адрес» объекта в памяти (куче, heap). Параметр метода получает
 * КОПИЮ ссылки: адрес тот же — объект тот же.
 *
 * Проверка: m02b/src/test/java/ex07/WalletOpsTest.java (убери @Disabled).
 *
 * ШАГ 1. deposit: добавь amount монет в кошелёк wallet.
 * Метод ничего не возвращает — но вызывающий код УВИДИТ изменение:
 * вы оба смотрите на один и тот же объект.
 *
 * ШАГ 2. richerOf: верни ссылку на тот кошелёк, где монет больше;
 * при равенстве — first. Заметь: ты возвращаешь не копию кошелька,
 * а ссылку на уже существующий объект.
 *
 * Метод tryToEmpty уже написан — его НЕ трогай. Разберись, почему
 * тест на него зелёный сразу: в этом и есть смысл упражнения.
 */
public class WalletOps {

    public static void deposit(Wallet wallet, int amount) {
        // TODO: допиши
    }

    public static Wallet richerOf(Wallet first, Wallet second) {
        // TODO: замени заглушку
        return null;
    }

    // НЕ МЕНЯЙ этот метод. Он переприсваивает ЛОКАЛЬНУЮ копию ссылки:
    // с этой строки параметр wallet указывает на НОВЫЙ пустой кошелёк,
    // а кошелёк вызывающего кода никто не трогал — до него метод
    // больше не дотянется.
    public static void tryToEmpty(Wallet wallet) {
        wallet = new Wallet();
    }
}
